# Generated CMakeLists override note:
# 在 `windows/runner/CMakeLists.txt` 中应将 BINARY_NAME 改为 hyrwbz，
# 并在 flutter pubspec 已就绪后由 `flutter create .` 自动生成完整 Windows runner。
# 此文件仅为说明性占位，实际由流水线 `flutter create .` 命令生成。
